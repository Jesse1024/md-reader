# 更新日志

本项目的所有重要变更都记录在此文件中。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增
- `uninstall.bat` / `uninstall.ps1` 卸载脚本，一键删除桌面快捷方式和安装目录。

## [1.2.0] - 2026-09-21

### 新增
- 顶栏「检查更新」按钮，自动对比 GitHub 最新 Release。
- 发现新版时可「立即更新」（下载并覆盖本地 index.html）或「下载安装包」。
- `update.bat` / `update.ps1` 脚本，双击全自动更新到最新版，无需选路径。
- 安装脚本现在会一并装入更新脚本。

## [1.1.0] - 2026-09-21

### 优化
- 载入时预生成小写文本缓存，检索免去每次扫描重复转换。
- 输入 150ms 防抖，打字过程不触发扫描，大语料下不卡顿。
- 命中超过 300 条时只渲染前 300 条，计数仍显示全部，避免巨型结果卡死页面。
- Enter 键先清防抖、立即执行检索。

## [1.0.0] - 2026-09-20

### 新增
- 段落级全文检索，跨文档列出命中内容段，关键词高亮，支持多词并含。
- 一键跳转到命中的具体段落，命中段高亮、自动滚动居中。
- 拖拽 md 文件读入（支持一次拖多个），选择文件夹递归读取。
- 自研轻量 Markdown 渲染：标题、列表、任务列表、引用、表格、代码块、链接、强调。
- 深浅主题切换，记忆偏好。
- 键盘操作：`/` 搜索、`↑↓` 切换结果、`Enter` 跳转、`Esc` 清空。
- `?q=关键词` URL 参数带入检索并定位。
- Windows 一键安装脚本（install.bat / install.ps1），生成桌面快捷方式。
